Analyze the requested CCG supervisor change.

Determine the smallest safe implementation that makes `review` explicitly use
`--claude-effort low` by default while preserving the review environment and
CLI overrides, leaves Codex unchanged, and avoids expanding analysis inputs.
Identify the focused tests and installation checks needed to prove the source
and installed supervisor agree. Return exactly Options, Recommendation, Risks,
and Validation sections.
