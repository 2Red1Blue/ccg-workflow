# Requirements

Replace ad hoc task path construction and shell-managed dual-model result collection with reusable CLI operations.

- One explicit task identity per selected project/workspace; matching retries reuse it and conflicting requests fail.
- Document and phase writes operate only on an existing validated task directory.
- Trellis remains sole task owner where configured; CCG remains the fallback.
- Analysis and review share durable process supervision, while their outputs remain semantically distinct.
- Package/install the task router and analysis command, synchronize active host guidance preserving creative-work exemptions.
- Preserve unrelated local Java workspace changes and all business source.
- Diagnose legacy orphan directories; do not silently merge or remove active task artifacts.
